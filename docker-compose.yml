services:
  weektasks:
    build: .
    container_name: weektasks
    restart: unless-stopped
    environment:
      - PORT=${PORT:-3000}
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - weektasks_data:/data

volumes:
  weektasks_data: